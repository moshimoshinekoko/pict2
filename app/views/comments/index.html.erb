<!-- ここにボタンだけ設置して、newの方にフォームを入れる？？ -->



<%= form_tag("comments/new") do %>
   <form>
     <div class="form-group">
       <textarea name="comment" class="form-control"></textarea>
       <textarea name="@topic.id" class="hidden"><%= @topic_id %></textarea>

     </div><!--form-group-->
     <button type="submit" class="btn btn-primary">コメント</button>
   <% end %>
